export enum CartStatusEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
  CHECKED_OUT = "checked_out",
  ORDERED = "ordered",
}

export enum OrderStatusEnum {
  PENDING = "pending",
  PROCESSING = "processing",
  SHIPPED = "shipped",
  DELIVERED = "delivered",
  CANCELLED = "cancelled",
  COMPLETED = "completed",
  REFUNDED = "refunded",
  RETURNED = "returned",
  ON_HOLD = "on_hold",
}
